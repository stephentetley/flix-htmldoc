# flix-htmldoc

A combinator library to generate HTML in Flix.

Note - v0.5.0 changes the module names / namespace to `HtmlDoc` 
for the top level module and `HtmlDoc.Tree` for the tree printer.

License: Apache 2.0

